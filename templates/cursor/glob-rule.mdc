---
description: TypeScript conventions (attach when TS/TSX files are in play)
globs: **/*.{ts,tsx}
alwaysApply: false
---

# TypeScript

- Enable and respect strict typing.
- Do not use `any`. Prefer `unknown` and narrow.
- Export named functions over default exports unless the framework requires otherwise.
- Colocate tests next to the file or under `tests/` using the repo's existing pattern.
