---
name: example-workflow
description: Use when running the team's example multi-step workflow (replace with a real trigger: "review a pull request", "cut a release", "generate a commit message").
---

# Example workflow

Agent Skills are for procedures that should not live in every session. Replace this example.

## When to use

- User asks to ...
- Agent is about to ...

## Steps

1. Read the relevant files.
2. Follow the repo's `AGENTS.md` commands.
3. Produce the output in the format below.

## Output

```markdown
- Summary:
- Checks run:
- Follow-ups:
```
