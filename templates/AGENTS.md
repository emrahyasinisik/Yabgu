# Project instructions for coding agents

Replace the placeholders. Keep this file short. Tool-specific notes belong in adapter files (`CLAUDE.md`, `.cursor/rules/`, `.github/copilot-instructions.md`), not here.

## Overview

- What this repo is:
- Primary languages / frameworks:
- Package manager:

## Commands

```bash
# install
# start
# test
# lint / typecheck
```

## Layout

- `src/` —
- `tests/` —

## Style

- Formatter / linter:
- Naming:
- Error handling:

## Testing

- When to add tests:
- How to run a single test:

## Do not

- Do not commit secrets or `.env` files
- Do not change public APIs without a matching test
- Do not add dependencies without a reason in the PR

## Pull requests

- Small diffs
- Describe why, not only what
- CI must be green
