@AGENTS.md

## Claude Code

Shared project rules live in `AGENTS.md`. Add only Claude-specific notes below.

- Use plan mode for refactors that touch more than one package.
- Prefer `.claude/rules/` for path-scoped conventions.
- Prefer `.claude/skills/` for multi-step workflows that should not sit in every session.
